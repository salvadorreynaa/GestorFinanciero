{
  "name": "finanzas-vaya-valla",
  "version": "1.0.0",
  "description": "Finanzas Vaya Valla Publicidad",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . FinanzasVayaValla --overwrite --platform=win32 --arch=x64 --icon=img/favicon.ico --prune=true --out=build"
  },
  "build": {
    "files": [
      "**/*"
    ]
  },
  "devDependencies": {
    "electron": "^36.2.0",
    "electron-packager": "^17.1.2"
  }
}
